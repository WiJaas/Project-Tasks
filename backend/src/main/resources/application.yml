server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/tasks_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
